---
export interface Props {
  className?: string;
}
const { className } = Astro.props;
---

<button
  class=`min-w-max text-white px-6 py-3 bg-pink-500 border-transparent hover:border-slate-700 rounded-md shadow-slate-500 outline-none relative overflow-hidden shadow-md border duration-300 ease-linear
after:absolute after:inset-x-0 after:aspect-square after:scale-0 after:opacity-70 after:origin-center after:duration-300 after:ease-linear after:rounded-full after:top-0 after:left-0 after:bg-slate-700 after:
 hover:after:opacity-100 hover:after:scale-[2.5] ${className}`
>
  <span class='flex justify-center relative z-[5]'> <slot /></span>
</button>
